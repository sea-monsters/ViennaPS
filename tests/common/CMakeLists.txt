cmake_minimum_required(VERSION 3.20)
# Shared helper scripts for reference-differential test runners.
# No build targets: this directory only hosts run_reference_oracle.ps1,
# consumed directly by the per-test run_paired_oracle.ps1 wrappers.

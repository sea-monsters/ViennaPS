diff --git a/include/viennals/lsAdvect.hpp b/include/viennals/lsAdvect.hpp
index 0d9ad9b..2512b0b 100644
--- a/include/viennals/lsAdvect.hpp
+++ b/include/viennals/lsAdvect.hpp
@@ -4,4 +4,8 @@
 
+#include <cmath>
+#include <exception>
 #include <functional>
 #include <limits>
+#include <string>
+#include <utility>
 #include <vector>
@@ -58,2 +62,46 @@ template <class T, int D> class Advect {
 
+public:
+  using LevelSetUpdateRate = std::pair<std::pair<T, T>, T>;
+  using LevelSetUpdateRates = std::vector<std::vector<LevelSetUpdateRate>>;
+
+  enum class LevelSetUpdateStatus { FALLBACK, HANDLED, ERROR };
+  enum class LevelSetUpdateFailurePolicy { FALLBACK, FAIL };
+
+  struct LevelSetUpdateContext {
+    const typename Domain<T, D>::DomainType &domain;
+    const LevelSetUpdateRates &rates;
+    double timeStep;
+    double integrationCutoff;
+    bool checkDissipation;
+    bool saveVelocities;
+  };
+
+  struct LevelSetUpdateOutput {
+    std::vector<std::vector<T>> values;
+    std::vector<std::vector<T>> velocities;
+    std::vector<std::vector<T>> dissipations;
+  };
+
+  using LevelSetUpdateExecutor = std::function<LevelSetUpdateStatus(
+      const LevelSetUpdateContext &, LevelSetUpdateOutput &, std::string &)>;
+
+  enum class LevelSetRebuildStatus { FALLBACK, HANDLED, ERROR };
+
+  struct LevelSetRebuildContext {
+    const typename Domain<T, D>::DomainType &domain;
+    T cutoff;
+    int finalWidth;
+    bool updatePointData;
+    bool saveVelocities;
+  };
+
+  struct LevelSetRebuildOutput {
+    SmartPointer<Domain<T, D>> domain;
+    std::vector<std::vector<unsigned>> sourceIds;
+  };
+
+  using LevelSetRebuildExecutor = std::function<LevelSetRebuildStatus(
+      const LevelSetRebuildContext &, LevelSetRebuildOutput &, std::string &)>;
+
+private:
   // Allow the time integration struct to access private members
@@ -83,2 +131,13 @@ template <class T, int D> class Advect {
       nullptr;
+  LevelSetUpdateExecutor levelSetUpdateExecutor = nullptr;
+  LevelSetUpdateFailurePolicy levelSetUpdateFailurePolicy =
+      LevelSetUpdateFailurePolicy::FALLBACK;
+  LevelSetRebuildExecutor levelSetRebuildExecutor = nullptr;
+  bool levelSetUpdateExecutorFailed = false;
+  std::string levelSetUpdateExecutorError;
+  bool levelSetRebuildExecutorFailed = false;
+  std::string levelSetRebuildExecutorError;
+  std::vector<SmartPointer<Domain<T, D>>> levelSetUpdateSnapshots;
+  bool advectionTimeError = false;
+  std::string advectionTimeErrorMessage;
 
@@ -86,3 +145,3 @@ template <class T, int D> class Advect {
   // corresponding velocity
-  std::vector<std::vector<std::pair<std::pair<T, T>, T>>> storedRates;
+  LevelSetUpdateRates storedRates;
   double currentTimeStep = -1.;
@@ -237,3 +296,129 @@ template <class T, int D> class Advect {
 
+  bool tryLevelSetRebuildExecutor(const T cutoff, const int finalWidth) {
+    if (!levelSetRebuildExecutor)
+      return false;
+
+    auto &currentLevelSet = levelSets.back();
+    const auto &oldDomain = currentLevelSet->getDomain();
+    LevelSetRebuildOutput output;
+    std::string executorError;
+    LevelSetRebuildStatus status = LevelSetRebuildStatus::ERROR;
+    try {
+      const LevelSetRebuildContext context{oldDomain, cutoff, finalWidth,
+                                           updatePointData,
+                                           saveAdvectionVelocities};
+      status = levelSetRebuildExecutor(context, output, executorError);
+    } catch (const std::exception &exception) {
+      executorError = exception.what();
+    } catch (...) {
+      executorError = "unknown exception";
+    }
+
+    if (status == LevelSetRebuildStatus::FALLBACK)
+      return false;
+
+    bool validOutput = status == LevelSetRebuildStatus::HANDLED &&
+                       output.domain != nullptr;
+    if (validOutput) {
+      const auto &replacementDomain = output.domain->getDomain();
+      const auto &replacementGrid = output.domain->getGrid();
+      const auto &currentGrid = currentLevelSet->getGrid();
+      validOutput = replacementGrid.getGridDelta() == currentGrid.getGridDelta() &&
+                    replacementGrid.getMinGridPoint() ==
+                        currentGrid.getMinGridPoint() &&
+                    replacementGrid.getMaxGridPoint() ==
+                        currentGrid.getMaxGridPoint();
+      for (unsigned i = 0; validOutput && i < D; ++i)
+        validOutput = replacementGrid.getBoundaryConditions(i) ==
+                      currentGrid.getBoundaryConditions(i);
+      validOutput = validOutput &&
+                    (!updatePointData ||
+                     replacementDomain.getNumberOfSegments() ==
+                         output.sourceIds.size());
+    }
+    if (validOutput && updatePointData) {
+      const auto &replacementDomain = output.domain->getDomain();
+      for (unsigned segment = 0;
+           segment < replacementDomain.getNumberOfSegments();
+           ++segment) {
+        const auto &replacementSegment =
+            replacementDomain.getDomainSegment(segment);
+        const auto &sourceIds = output.sourceIds[segment];
+        validOutput = sourceIds.size() == replacementSegment.definedValues.size();
+        if (updatePointData)
+          for (const auto sourceId : sourceIds)
+            validOutput = validOutput &&
+                          sourceId < oldDomain.getNumberOfPoints();
+      }
+    }
+
+    if (!validOutput) {
+      if (executorError.empty())
+        executorError = "invalid rebuild domain, grid, or source-ID map";
+      if (levelSetUpdateFailurePolicy == LevelSetUpdateFailurePolicy::FAIL) {
+        levelSetRebuildExecutorFailed = true;
+        levelSetRebuildExecutorError = executorError;
+        return true;
+      }
+      VIENNACORE_LOG_WARNING(
+          "Level Set rebuild executor failed; using CPU fallback: " +
+          executorError);
+      return false;
+    }
+
+    // Translate all existing scalar/vector point data into a local replacement
+    // before publishing the new domain. This keeps commit atomic on errors.
+    try {
+      auto translatedDomain = SmartPointer<Domain<T, D>>::New(output.domain);
+      translatedDomain->getPointData().clear();
+      if (updatePointData) {
+        translatedDomain->getPointData().translateFromMultiData(
+            currentLevelSet->getPointData(), output.sourceIds);
+      }
+      translatedDomain->finalize(finalWidth);
+      auto commitSnapshot = SmartPointer<Domain<T, D>>::New(currentLevelSet);
+      try {
+        currentLevelSet->deepCopy(translatedDomain);
+      } catch (...) {
+        try {
+          currentLevelSet->deepCopy(commitSnapshot);
+        } catch (...) {
+        }
+        throw;
+      }
+      return true;
+    } catch (const std::exception &exception) {
+      executorError = exception.what();
+    } catch (...) {
+      executorError = "unknown exception";
+    }
+    if (levelSetUpdateFailurePolicy == LevelSetUpdateFailurePolicy::FAIL) {
+      levelSetRebuildExecutorFailed = true;
+      levelSetRebuildExecutorError = executorError;
+      return true;
+    }
+    VIENNACORE_LOG_WARNING(
+        "Level Set rebuild executor failed; using CPU fallback: " +
+        executorError);
+    return false;
+  }
+
   void rebuildLS() {
+    if (levelSetUpdateExecutorFailed || levelSetRebuildExecutorFailed)
+      return;
+
+    T cutoff = 1.0;
+    int finalWidth = 2;
+    if (spatialScheme ==
+        SpatialSchemeEnum::STENCIL_LOCAL_LAX_FRIEDRICHS_1ST_ORDER) {
+      cutoff = 1.5;
+      finalWidth = 3;
+    }
+
+    if (tryLevelSetRebuildExecutor(cutoff, finalWidth)) {
+      if (levelSetRebuildExecutorFailed)
+        return;
+      return;
+    }
+
     // TODO: this function uses Manhattan distances for renormalisation,
@@ -249,10 +434,2 @@ template <class T, int D> class Advect {
     // immediate re-expansion
-    T cutoff = 1.0;
-    int finalWidth = 2;
-    if (spatialScheme ==
-        SpatialSchemeEnum::STENCIL_LOCAL_LAX_FRIEDRICHS_1ST_ORDER) {
-      cutoff = 1.5;
-      finalWidth = 3;
-    }
-
     newDomain.initialize(domain.getNewSegmentation(),
@@ -683,2 +860,115 @@ template <class T, int D> class Advect {
 
+  void captureLevelSetUpdateSnapshot() {
+    if ((!levelSetUpdateExecutor && !levelSetRebuildExecutor) ||
+        levelSetUpdateFailurePolicy != LevelSetUpdateFailurePolicy::FAIL ||
+        !levelSetUpdateSnapshots.empty())
+      return;
+
+    levelSetUpdateSnapshots.reserve(levelSets.size());
+    for (const auto &levelSet : levelSets) {
+      levelSetUpdateSnapshots.push_back(
+          SmartPointer<Domain<T, D>>::New(levelSet));
+    }
+  }
+
+  void restoreLevelSetUpdateSnapshot() {
+    if (levelSetUpdateSnapshots.size() == levelSets.size()) {
+      for (std::size_t i = 0; i < levelSets.size(); ++i)
+        levelSets[i]->deepCopy(levelSetUpdateSnapshots[i]);
+    }
+    levelSetUpdateSnapshots.clear();
+  }
+
+  void discardLevelSetUpdateSnapshot() { levelSetUpdateSnapshots.clear(); }
+
+  bool tryLevelSetUpdateExecutor(const double dt) {
+    if (!levelSetUpdateExecutor)
+      return false;
+
+    auto &topDomain = levelSets.back()->getDomain();
+    LevelSetUpdateOutput output;
+    std::string executorError;
+    LevelSetUpdateStatus status = LevelSetUpdateStatus::ERROR;
+    try {
+      const LevelSetUpdateContext context{
+          topDomain,         storedRates,      dt,
+          integrationCutoff, checkDissipation, saveAdvectionVelocities};
+      status = levelSetUpdateExecutor(context, output, executorError);
+    } catch (const std::exception &exception) {
+      executorError = exception.what();
+    } catch (...) {
+      executorError = "unknown exception";
+    }
+
+    if (status == LevelSetUpdateStatus::FALLBACK)
+      return false;
+
+    bool validOutput = status == LevelSetUpdateStatus::HANDLED &&
+                       output.values.size() == topDomain.getNumberOfSegments();
+    if (saveAdvectionVelocities) {
+      validOutput =
+          validOutput &&
+          output.velocities.size() == topDomain.getNumberOfSegments() &&
+          output.dissipations.size() == topDomain.getNumberOfSegments();
+    }
+
+    for (unsigned p = 0; validOutput && p < topDomain.getNumberOfSegments();
+         ++p) {
+      const auto pointCount = topDomain.getDomainSegment(p).getNumberOfPoints();
+      validOutput = output.values[p].size() == pointCount;
+      if (saveAdvectionVelocities) {
+        validOutput = validOutput &&
+                      output.velocities[p].size() == pointCount &&
+                      output.dissipations[p].size() == pointCount;
+      }
+      for (const auto value : output.values[p])
+        validOutput = validOutput && std::isfinite(value);
+      if (saveAdvectionVelocities) {
+        for (const auto velocity : output.velocities[p])
+          validOutput = validOutput && std::isfinite(velocity);
+        for (const auto dissipation : output.dissipations[p])
+          validOutput = validOutput && std::isfinite(dissipation);
+      }
+    }
+
+    if (!validOutput) {
+      if (executorError.empty())
+        executorError = "invalid output shape or non-finite value";
+      if (levelSetUpdateFailurePolicy == LevelSetUpdateFailurePolicy::FAIL) {
+        levelSetUpdateExecutorFailed = true;
+        levelSetUpdateExecutorError = executorError;
+        return true;
+      }
+      VIENNACORE_LOG_WARNING(
+          "Level Set update executor failed; using CPU fallback: " +
+          executorError);
+      return false;
+    }
+
+    typename PointData<T>::ScalarDataType velocities;
+    typename PointData<T>::ScalarDataType dissipations;
+    if (saveAdvectionVelocities) {
+      for (unsigned p = 0; p < output.velocities.size(); ++p) {
+        velocities.insert(velocities.end(),
+                          std::make_move_iterator(output.velocities[p].begin()),
+                          std::make_move_iterator(output.velocities[p].end()));
+        dissipations.insert(
+            dissipations.end(),
+            std::make_move_iterator(output.dissipations[p].begin()),
+            std::make_move_iterator(output.dissipations[p].end()));
+      }
+    }
+
+    for (unsigned p = 0; p < topDomain.getNumberOfSegments(); ++p) {
+      topDomain.getDomainSegment(p).definedValues = std::move(output.values[p]);
+    }
+    if (saveAdvectionVelocities) {
+      auto &pointData = levelSets.back()->getPointData();
+      pointData.insertReplaceScalarData(std::move(velocities), velocityLabel);
+      pointData.insertReplaceScalarData(std::move(dissipations),
+                                        dissipationLabel);
+    }
+    return true;
+  }
+
   // Level Sets below are also considered in order to adjust the advection
@@ -701,2 +991,7 @@ template <class T, int D> class Advect {
 
+    if (tryLevelSetUpdateExecutor(dt)) {
+      storedRates.clear();
+      return;
+    }
+
     const bool saveVelocities = saveAdvectionVelocities;
@@ -802,2 +1097,5 @@ template <class T, int D> class Advect {
   void adjustLowerLayers() {
+    if (levelSetUpdateExecutorFailed || levelSetRebuildExecutorFailed)
+      return;
+
     // Adjust all level sets below the advected one
@@ -831,2 +1129,28 @@ template <class T, int D> class Advect {
 
+  void setAdvectionTimeError(std::string message) {
+    advectionTimeError = true;
+    advectionTimeErrorMessage = std::move(message);
+  }
+
+  bool isZeroVelocityTimeStep(const double timeStep) const {
+    return timeStep == std::numeric_limits<double>::max() ||
+           timeStep == static_cast<double>(std::numeric_limits<T>::max());
+  }
+
+  bool validateAdvectionTimeBeforeUpdate() {
+    if (isZeroVelocityTimeStep(currentTimeStep))
+      return false;
+
+    if (std::isfinite(currentTimeStep) && currentTimeStep > 0.0)
+      return true;
+
+    if (!performOnlySingleStep) {
+      setAdvectionTimeError(
+          std::isfinite(currentTimeStep) && currentTimeStep == 0.0
+              ? "Advection made no time progress."
+              : "Advection produced an invalid time step.");
+    }
+    return false;
+  }
+
 public:
@@ -970,2 +1294,43 @@ public:
 
+  /// Set an optional executor for the value-update phase between rate
+  /// calculation and sparse-domain rebuild. Returning FALLBACK uses the
+  /// built-in CPU implementation; ERROR uses it only in FALLBACK policy mode.
+  void setLevelSetUpdateExecutor(LevelSetUpdateExecutor executor) {
+    levelSetUpdateExecutor = std::move(executor);
+  }
+
+  /// Set an optional executor for sparse-domain rebuild. Returning FALLBACK
+  /// uses the built-in CPU implementation; ERROR uses it only in FALLBACK
+  /// policy mode.
+  void setLevelSetRebuildExecutor(LevelSetRebuildExecutor executor) {
+    levelSetRebuildExecutor = std::move(executor);
+  }
+
+  void setLevelSetUpdateFailurePolicy(
+      LevelSetUpdateFailurePolicy policy) {
+    levelSetUpdateFailurePolicy = policy;
+  }
+
+  bool hasLevelSetUpdateError() const {
+    return levelSetUpdateExecutorFailed;
+  }
+
+  const std::string &getLevelSetUpdateError() const {
+    return levelSetUpdateExecutorError;
+  }
+
+  bool hasLevelSetRebuildError() const {
+    return levelSetRebuildExecutorFailed;
+  }
+
+  const std::string &getLevelSetRebuildError() const {
+    return levelSetRebuildExecutorError;
+  }
+
+  bool hasAdvectionTimeError() const { return advectionTimeError; }
+
+  const std::string &getAdvectionTimeError() const {
+    return advectionTimeErrorMessage;
+  }
+
   // Prepare the levelset for advection, based on the provided spatial
@@ -979,2 +1344,4 @@ public:
 
+    captureLevelSetUpdateSnapshot();
+
     if (spatialScheme == SpatialSchemeEnum::ENGQUIST_OSHER_1ST_ORDER) {
@@ -1017,4 +1384,12 @@ public:
   void apply() {
+    levelSetUpdateExecutorFailed = false;
+    levelSetUpdateExecutorError.clear();
+    levelSetRebuildExecutorFailed = false;
+    levelSetRebuildExecutorError.clear();
+    advectionTimeError = false;
+    advectionTimeErrorMessage.clear();
+
     // check whether a level set and velocities have been given
     if (levelSets.empty()) {
+      discardLevelSetUpdateSnapshot();
       VIENNACORE_LOG_ERROR("No level sets passed to Advect. Not advecting.");
@@ -1023,2 +1398,3 @@ public:
     if (velocities == nullptr) {
+      discardLevelSetUpdateSnapshot();
       VIENNACORE_LOG_ERROR(
@@ -1028,5 +1404,40 @@ public:
 
+    captureLevelSetUpdateSnapshot();
+
     if (advectionTime == 0.) {
-      advectedTime = advect(std::numeric_limits<double>::max());
+      const double timeStep = advect(std::numeric_limits<double>::max());
+      numberOfTimeSteps = 0;
+      if (levelSetUpdateExecutorFailed || levelSetRebuildExecutorFailed) {
+        restoreLevelSetUpdateSnapshot();
+        advectedTime = 0.0;
+        return;
+      }
+      if (advectionTimeError) {
+        discardLevelSetUpdateSnapshot();
+        advectedTime = 0.0;
+        return;
+      }
+      if (isZeroVelocityTimeStep(timeStep)) {
+        discardLevelSetUpdateSnapshot();
+        advectedTime = timeStep;
+        numberOfTimeSteps = 1;
+        return;
+      }
+      const double nextTime = timeStep;
+      const bool validTime = std::isfinite(timeStep) && timeStep >= 0.0 &&
+                             std::isfinite(nextTime);
+      const bool madeProgress = nextTime > 0.0;
+      if (!validTime || (!performOnlySingleStep && !madeProgress)) {
+        if (!performOnlySingleStep) {
+          setAdvectionTimeError(
+              validTime ? "Advection made no time progress."
+                        : "Advection produced an invalid time step.");
+          discardLevelSetUpdateSnapshot();
+          advectedTime = 0.0;
+          return;
+        }
+      }
+      advectedTime = timeStep;
       numberOfTimeSteps = 1;
+      discardLevelSetUpdateSnapshot();
     } else {
@@ -1035,3 +1446,42 @@ public:
       while (currentTime < advectionTime) {
-        currentTime += advect(advectionTime - currentTime);
+        const double timeStep = advect(advectionTime - currentTime);
+        if (levelSetUpdateExecutorFailed || levelSetRebuildExecutorFailed)
+          break;
+        if (advectionTimeError)
+          break;
+        if (isZeroVelocityTimeStep(timeStep)) {
+          currentTime = timeStep;
+          ++numberOfTimeSteps;
+          break;
+        }
+        if (!std::isfinite(timeStep) || timeStep < 0.0) {
+          if (performOnlySingleStep) {
+            currentTime = timeStep;
+            ++numberOfTimeSteps;
+            break;
+          }
+          setAdvectionTimeError("Advection produced an invalid time step.");
+          break;
+        }
+        const double nextTime = currentTime + timeStep;
+        if (!std::isfinite(nextTime)) {
+          if (performOnlySingleStep) {
+            currentTime = nextTime;
+            ++numberOfTimeSteps;
+            break;
+          }
+          setAdvectionTimeError(
+              "Advection produced a non-finite accumulated time.");
+          break;
+        }
+        if (!(nextTime > currentTime)) {
+          if (performOnlySingleStep) {
+            currentTime = nextTime;
+            ++numberOfTimeSteps;
+            break;
+          }
+          setAdvectionTimeError("Advection made no time progress.");
+          break;
+        }
+        currentTime = nextTime;
         ++numberOfTimeSteps;
@@ -1040,2 +1490,8 @@ public:
       }
+      if (levelSetUpdateExecutorFailed || levelSetRebuildExecutorFailed) {
+        restoreLevelSetUpdateSnapshot();
+        currentTime = 0.0;
+      } else {
+        discardLevelSetUpdateSnapshot();
+      }
       advectedTime = currentTime;
diff --git a/include/viennals/lsAdvectIntegrationSchemes.hpp b/include/viennals/lsAdvectIntegrationSchemes.hpp
index dc28b3e..123beb2 100644
--- a/include/viennals/lsAdvectIntegrationSchemes.hpp
+++ b/include/viennals/lsAdvectIntegrationSchemes.hpp
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cmath>
+#include <limits>
+
 #include <lsBooleanOperation.hpp>
 #include <lsDomain.hpp>
 
@@ -48,10 +51,22 @@ template <class T, int D> struct AdvectTimeIntegration {
     if (kernel.currentTimeStep < 0. || kernel.storedRates.empty())
       kernel.computeRates(maxTimeStep);
 
+    if (!kernel.validateAdvectionTimeBeforeUpdate()) {
+      const double rejectedTimeStep = kernel.currentTimeStep;
+      kernel.storedRates.clear();
+      return rejectedTimeStep;
+    }
+
     kernel.updateLevelSet(kernel.currentTimeStep);
 
+    if (kernel.hasLevelSetUpdateError())
+      return 0.;
+
     kernel.rebuildLS();
 
+    if (kernel.hasLevelSetRebuildError())
+      return 0.;
+
     if (updateLowerLayers)
       kernel.adjustLowerLayers();
 
@@ -82,9 +97,19 @@ template <class T, int D> struct AdvectTimeIntegration {
     double dt1 = evolveForwardEuler(kernel, maxTimeStep,
                                     kernel.velocityUpdateCallback != nullptr);
 
-    if (dt1 <= 0.)
+    if (kernel.hasLevelSetUpdateError() || kernel.hasLevelSetRebuildError() ||
+        kernel.hasAdvectionTimeError())
+      return dt1;
+
+    if (!std::isfinite(dt1) || dt1 < 0.)
+      return dt1;
+
+    if (dt1 == 0.)
       return 0.;
 
+    if (kernel.isZeroVelocityTimeStep(dt1))
+      return dt1;
+
     if (kernel.velocityUpdateCallback)
       kernel.velocityUpdateCallback(kernel.levelSets.back());
 
@@ -93,6 +118,16 @@ template <class T, int D> struct AdvectTimeIntegration {
     // Update to u* = u^(1) + dt * L(u^(1))
     double dt2 = evolveForwardEuler(kernel, dt1, false);
 
+    if (kernel.hasLevelSetUpdateError() || kernel.hasLevelSetRebuildError() ||
+        kernel.hasAdvectionTimeError())
+      return 0.;
+
+    if (!std::isfinite(dt2) || dt2 <= 0.)
+      return dt2;
+
+    if (kernel.isZeroVelocityTimeStep(dt2))
+      return dt2;
+
     // Combine: u^(n+1) = 0.5 * u^n + 0.5 * u*
     bool etched = kernel.combineLevelSets(0.5, 0.5);
 
@@ -123,9 +158,19 @@ template <class T, int D> struct AdvectTimeIntegration {
     double dt1 = evolveForwardEuler(kernel, maxTimeStep,
                                     kernel.velocityUpdateCallback != nullptr);
 
-    if (dt1 <= 0.)
+    if (kernel.hasLevelSetUpdateError() || kernel.hasLevelSetRebuildError() ||
+        kernel.hasAdvectionTimeError())
+      return dt1;
+
+    if (!std::isfinite(dt1) || dt1 < 0.)
+      return dt1;
+
+    if (dt1 == 0.)
       return 0.;
 
+    if (kernel.isZeroVelocityTimeStep(dt1))
+      return dt1;
+
     if (kernel.velocityUpdateCallback)
       kernel.velocityUpdateCallback(kernel.levelSets.back());
 
@@ -133,6 +178,16 @@ template <class T, int D> struct AdvectTimeIntegration {
     // u* = u^(1) + dt * L(u^(1))
     double dt2 = evolveForwardEuler(kernel, dt1, false);
 
+    if (kernel.hasLevelSetUpdateError() || kernel.hasLevelSetRebuildError() ||
+        kernel.hasAdvectionTimeError())
+      return 0.;
+
+    if (!std::isfinite(dt2) || dt2 <= 0.)
+      return dt2;
+
+    if (kernel.isZeroVelocityTimeStep(dt2))
+      return dt2;
+
     // Combine to get u^(2) = 0.75 * u^n + 0.25 * u*.
     bool etched1 = kernel.combineLevelSets(0.75, 0.25);
 
@@ -153,6 +208,16 @@ template <class T, int D> struct AdvectTimeIntegration {
     // u** = u^(2) + dt * L(u^(2))
     double dt3 = evolveForwardEuler(kernel, dt1, false);
 
+    if (kernel.hasLevelSetUpdateError() ||
+        kernel.hasAdvectionTimeError())
+      return 0.;
+
+    if (!std::isfinite(dt3) || dt3 <= 0.)
+      return dt3;
+
+    if (kernel.isZeroVelocityTimeStep(dt3))
+      return dt3;
+
     // Combine to get u^(n+1) = 1/3 * u^n + 2/3 * u**.
     bool etched2 = kernel.combineLevelSets(1.0 / 3.0, 2.0 / 3.0);
